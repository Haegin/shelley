echo "Running bash"
source .shelley

# Aliases shorten common commands
alias ll="ls -l"
alias la="ls -la"

# You can also use other aliases in your aliases
alias lh="ll -h"

# They're also handy if you often mistype things
alias it=git
alias tig=git
