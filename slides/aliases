title aliases

echo; read; bullet Aliases let you set your own abbreviations

echo; read; bullet Add these to \~/.bashrc to make them available in every shell

echo; read; bullet Can be used for shortcuts

echo; pygmentize -g .bashrc | tail -n 7 | head -n 3 | indent ; echo -e "\e[1;37m"

read; bullet And to correct common typos

echo; pygmentize -g .bashrc | tail -n 4 | head -n 2 | indent ; echo -e "\e[1;37m"

read; clear; title git aliases

bullet Git also has aliases
read; echo; bullet These are stored in the \~/.gitconfig file
read; echo; pygmentize -l ini .gitconfig | indent ; echo -e "\e[1;37m"

read; next
